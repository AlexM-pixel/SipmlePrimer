<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingBottom="24dp">

    <!-- Системная ручка для смахивания шторки вниз -->
    <com.google.android.material.bottomsheet.BottomSheetDragHandleView
        android:id="@+id/drag_handle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tint="#D0D0D0" />

    <!-- Заголовок диалога -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:text="Выберите счет"
        android:textColor="#333333"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- СПИСОК КАРТ -->
    <!-- layout_weight="1" не даст списку вытолкнуть кнопку за пределы экрана -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_sheet_cards"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="16dp"
        tools:listitem="@layout/item_sheet_card" />
    <!-- tools:listitem покажет тебе превью списка прямо в редакторе Android Studio -->

    <!-- КНОПКА ГОТОВО (Аналог оранжевой кнопки Done с твоего референса) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_done"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:backgroundTint="#6FCF97"
        android:text="Выбрать"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        app:cornerRadius="16dp" />

</LinearLayout>